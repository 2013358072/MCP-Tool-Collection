[project]
name = "mcp-tool-collection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anyio>=4.12.1",
    "asyncer>=0.0.17",
    "fastmcp>=3.1.0",
    "mcp[cli]>=1.26.0",
]
